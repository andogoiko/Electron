{
  "name": "platzipics",
  "version": "1.0.0",
  "description": "aplicativo de electron para visualización de imágenes",
  "scripts": {
    "build": "electron src/index.js",
    "lint": "standard --fix",
    "prebuild": "parcel build src/renderer/*.html --public-url ./",
    "start": "parcel serve src/renderer/*.html --open --public-url ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "electron": "^21.2.2",
    "photonkit": "^0.1.2"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "os-browserify": "^0.3.0",
    "parcel": "^2.7.0",
    "path-browserify": "^1.0.1",
    "process": "^0.11.10",
    "querystring-es3": "^0.2.1",
    "standard": "^17.0.0",
    "url": "^0.11.0"
  }
}
